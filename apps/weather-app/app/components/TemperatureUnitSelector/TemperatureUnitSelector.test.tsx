describe("TemperatureSelector", () => {
  it.todo(
    "Selecting a temperature sends an update to the TemperatureUnit context"
  );

  it.todo("Selecting a temperature changes which radio is selected");
});
